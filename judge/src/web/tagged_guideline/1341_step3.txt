[block0]
사용자로부터 x, y 값을 입력받고 로그 x 값의 합, 총 y 값, 로그 x 제곱의 합, 로그 y * 로그 x의 합을 계산하는 과정.

[block1]
로그 x 값의 합, 총 y 값, 로그 x 제곱의 합, 로그 y * 로그 x의 합을 기반으로 a와 b 계수를 계산하는 과정.

[block2]
a와 b 계수를 입력받아 입력 x에 대한 예측값을 계산하는 과정.

[block3]
프로그램의 시작점을 설정하고 x, y 값을 읽어오는 과정. a와 b 계수를 계산하고 입력 x에 대한 예측값을 출력하는 과정.